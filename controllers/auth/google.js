// const { User } = require('../../models');
// const { generateTokens } = require('../../helpers');

// const google = async (req, res) => {

//   const { _id } = req.user;
//   const { token, refreshToken } = await generateTokens(_id);
//   await User.findByIdAndUpdate(_id, { token, refreshToken });
//   res.redirect(`https://kirill-filonchuk.github.io/pet_team02/login?token=${token}`);
// };

// module.exports = google;

//   res.redirect(`https://tiny-hare-cowboy-hat.cyclic.app/login?token=${token}`);
// https://tiny-hare-cowboy-hat.cyclic.app/
// https://kirill-filonchuk.github.io/pet_team02/
